---
title: "Test"
author: "Eric Wu"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r libraries}
rm(list = ls()) # Clean environment
library(reticulate)
library(tidyverse)
```

## Data Cleaning Relating to Player Statistics

```{r}
players <- read.csv("player_data_non_duplicate.csv")
contract <- read.csv("contracts.csv")

```

```{r}
test <- full_join(contract, players, by = join_by(Name == Player), keep = TRUE, relationship = "many-to-many")
```

```{r}
tests <- na.omit(test)

number <- unique(tests$Name)

write.csv(movies,"C:\\Users\\wue77\\Documents\\GitHub\\DS-320_Project\\Combined Contract and Player Data\\tests.csv", row.names = FALSE)
```
